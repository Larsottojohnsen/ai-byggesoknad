[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libxml2", "libxslt", "pango", "cairo", "gdk-pixbuf", "gobject-introspection", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT --workers 2"
